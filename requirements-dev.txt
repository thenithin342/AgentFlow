# Test-only dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# CAUTION: the `~=0.24.0` pin on pytest-asyncio is intentional — the
# 0.21 → 0.23 transition introduced breaking changes to the `event_loop`
# fixture and async-mode config that silently break existing test suites.
# Pinning to 0.24.x freezes the working range.
#
# If you re-install on an environment that already has a newer
# pytest-asyncio (e.g. 1.x), pip will downgrade. Verify the test suite
# still passes after the downgrade.

-r requirements.txt

pytest>=8.0.0
pytest-asyncio~=0.24.0

# FastAPI in-process smoke tests. httpx >= 0.27 ships the stable
# `ASGITransport` (no httpx-sgi package needed) and asgi-lifespan
# runs the FastAPI `lifespan` context without a real socket.
httpx>=0.27
asgi-lifespan>=2.1
