# AgentFlow — local development & production-deploy topology.
#
# Two services:
#   - postgres   — durable checkpointer backend (opt-in via --profile postgres)
#   - backend    — the FastAPI app, built from ./Dockerfile
#
# Reference: DEPLOYMENT.md section 3.3 "docker-compose topology".
#
# Dev (SQLite, no Postgres):
#   docker-compose up backend
#
# Dev + Postgres:
#   docker-compose --profile postgres up
#
# Production single-node (requires POSTGRES_CONN_STRING set in .env):
#   docker-compose --profile postgres up -d

services:
  # ---------------------------------------------------------------------------
  # Postgres — checkpointer backend (opt-in; use --profile postgres)
  # ---------------------------------------------------------------------------
  postgres:
    profiles: [postgres]
    image: postgres:16-alpine
    container_name: agentflow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-agentflow}
      # POSTGRES_PASSWORD must be set explicitly — no insecure default.
      # For local dev: add POSTGRES_PASSWORD=<your-local-pass> to .env
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set (see .env.example)}
      POSTGRES_DB: ${POSTGRES_DB:-agentflow}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      # Bind to localhost only so the dev DB is not exposed on the LAN.
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-agentflow} -d ${POSTGRES_DB:-agentflow}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  # ---------------------------------------------------------------------------
  # Backend — FastAPI app
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentflow-backend
    restart: unless-stopped
    # Only wait for postgres when the postgres profile is active.
    depends_on:
      postgres:
        condition: service_healthy
        required: false
    environment:
      # --- Required at runtime ---
      GROQ_API_KEY: ${GROQ_API_KEY:?GROQ_API_KEY is required}
      TAVILY_API_KEY: ${TAVILY_API_KEY:?TAVILY_API_KEY is required}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set to a 32+ byte secret in production}
      # --- Database ---
      # When POSTGRES_CONN_STRING is set the app uses Postgres; otherwise SQLite.
      # Accept it from the environment (e.g. Railway auto-inject or .env).
      # Do NOT construct it here — passwords may contain URI-reserved chars.
      POSTGRES_CONN_STRING: ${POSTGRES_CONN_STRING:-}
      CHECKPOINT_DB_PATH: /app/agentflow.db
      # --- Persistence dirs (mounted as volumes so data survives restarts) ---
      DATA_DIR: /app/data
      FAISS_INDEX_DIR: /app/faiss_indexes
      LTM_INDEX_DIR: /app/ltm_indexes
      # --- Optional: LangSmith tracing ---
      LANGSMITH_API_KEY: ${LANGSMITH_API_KEY:-}
      LANGSMITH_TRACING: ${LANGSMITH_TRACING:-false}
      LANGSMITH_PROJECT: ${LANGSMITH_PROJECT:-AgentFlow}
      # --- CORS ---
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:5174}
      # --- Log level ---
      LOG_LEVEL: ${LOG_LEVEL:-info}
    volumes:
      - backend_data:/app/data
      - backend_faiss:/app/faiss_indexes
      - backend_ltm:/app/ltm_indexes
      # Mount the HMAC secret file so it persists across container restarts.
      # Create an empty file first:  touch .faiss_secret
      - ./.faiss_secret:/app/.faiss_secret
    ports:
      - "127.0.0.1:8000:8000"
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "--max-time", "3", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          memory: 1.5G
        reservations:
          memory: 512M

volumes:
  postgres_data:
    driver: local
  backend_data:
    driver: local
  backend_faiss:
    driver: local
  backend_ltm:
    driver: local
